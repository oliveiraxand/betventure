{
  "scripts": {
    "build": "npx tsc",
    "migrate": "npx tsc && npx sequelize-cli db:migrate",
    "seed": "npx tsc && npx sequelize-cli db:seed:all",
    "test:mocha": "npm run test:mocha --prefix backend",
    "test:jest": "jest /home/alexandre/Workspace/betventure/backend/tests",
    "start": "npx tsc && node build/src/server.js"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "dependencies": {
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.5",
    "express": "^4.18.2",
    "sequelize": "^6.35.1",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/jest": "^29.5.10",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0"
  }
}
